name: ocsf-schema
repo: ocsf-schema
description: >
  Query.ai's fork of the Open Cybersecurity Schema Framework (OCSF) core schema:
  categories, event classes, data types, and the attribute dictionary. Vendored as a git
  submodule by queryai-ocsf and iac-lambda-metadata to drive OCSF code generation. Content
  repo — deploys nothing itself; consumed as a build-time submodule input.

deployment:
  type: none
