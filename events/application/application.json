{
  "caption": "Application Activity",
  "category": "application",
  "description": "The Application Activity event is a generic event that defines a set of attributes available in the application events. As a generic event, it could be used to log events that are not otherwise defined by the Application Activity category.",
  "extends": "base_event",
  "name": "application",
  "profiles": [
    "host"
  ],
  "attributes": {
    "$include": [
      "profiles/host.json"
    ]
  }
}
