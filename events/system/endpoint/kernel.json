{
  "attributes": {
    "activity_id": {
      "$include": "enums/kernel_activity.json"
    },
    "kernel": {
      "description": "The target kernel resource.",
      "group": "primary",
      "requirement": "required"
    }
  },
  "description": "Endpoint Kernel Activity events report when an process creates, reads, or deletes a kernel resource.",
  "extends": "endpoint_activity",
  "name": "Endpoint Kernel Activity",
  "type": "endpoint_kernel_activity",
  "uid": 8
}