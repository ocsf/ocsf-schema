{
    "$id": "https://schema.ocsf.io/observable.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "References",
    "description": "A list of references. A reference is a hyperlink to an associated definition for the current item (attribute, event class, or object), for example where an equivalent is defined in MITRE d3fend. Note: use `reference` or `references` but not both (this would be redundant).",
    "type": "array",
    "items": {
        "$ref": "reference.schema.json"
    }
}